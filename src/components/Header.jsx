import { Typography } from "@mui/material";


const Header = () => {
  return (
    <div>
      <Typography
        align="center"
        variant="h4"
        sx={{
          fontWeight: "bold", 
        }}
      >
        Deal Widgets
      </Typography>
      {/* checking */}

    </div>
  );
};

export default Header;
